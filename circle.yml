machine:
  java:
    version: openjdk8
  timezone: America/Sao_Paulo # Set the timezone

  test:
    override:
      - mvn clean install
  general:
    artifacts:
      - "/home/ubuntu/circleproject2/target/SampleJerseyApp-1.0-SNAPSHOT.war"
