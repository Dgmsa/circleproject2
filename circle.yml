machine:
  java:
    version: openjdk8
  timezone: America/Sao_Paulo # Set the timezone

  test:
    override:
      - mvn clean install
      - cp *.war /tmp/hsperfdata_ubuntu/
  general:
    artifacts:
      - "target/"
