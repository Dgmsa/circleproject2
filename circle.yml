machine:
  java:
    version: openjdk8
  timezone: America/Sao_Paulo # Set the timezone

  test:
    override:
      - mvn clean install
      - pwd
      - mkdir $CIRCLE_ARTIFACTS/target
      - cp circleproject2/target/*.war $CIRCLE_ARTIFACTS/target/
  general:
    artifacts:
      - "target/"
