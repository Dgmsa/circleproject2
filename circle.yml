machine:
  java:
    version: openjdk8
  timezone: America/Sao_Paulo # Set the timezone

  test:
    override:
      - mvn clean install
      - mkdir $CIRCLE_ARTIFACTS/target
      - cp target/*.war $CIRCLE_ARTIFACTS/target/
  general:
    artifacts:
      - "target/"
