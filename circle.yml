machine:
  java:
    version: openjdk8
  #timezone: America/Sao_Paulo # Set the timezone

  test:
    override:
      - mvn clean install
      - mkdir $CIRCLE_ARTIFACTS/target
      - mv target/*.war $CIRCLE_ARTIFACTS/target
      - mv /home/ubuntu/circleproject2/target/SampleJerseyApp-1.0-SNAPSHOT.war $CIRCLE_ARTIFACTS/target/
      - touch $CIRCLE_ARTIFACTS/teste.txt
  general:
    artifacts:
      - "/target/SampleJerseyApp-1.0-SNAPSHOT.war"
